/** @format */
export const trim: (input: string) => string = (input: string) => input.trim()

export const charAt: (input: string, position: number) => string = (
  input: string,
  position: number,
): string => input.charAt(position)
